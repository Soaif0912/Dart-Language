void main() {
  var name =
      "Soaif"; // using var keyword we can assign any datatype value to the variable, and the datatype value we are assigning at the variable initialization that datatype will be set to the variable.

  // name = 10; // will thorw error

  var a; // Because here we didn't assigned any value to the variable, then we can assign any value later.

  a = 10;
  print(a);

  a = "hello";
  print(a);

  a = true;
  print(a);
}
